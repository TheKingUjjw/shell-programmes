# Write a shell script that accepts one or more file name as arguments and
# convert the file contents to uppercase, provided they exist in the current
# directory

#! /usr/bin/bash

a=1; b=2; c=3;
d=$(( ++a**b*c++ + a ))
echo $d
exit 0
